## 中文
- 为多个工具面板加入默认自动转换体验：输入变化后自动执行转换或格式化，减少手动点击步骤，覆盖 JSON、格式转换、Base64、URL、Hash、SQL、XML、正则、Cron、二维码等工具。
- 优化时间戳与雪花 ID 工具的初始状态：打开后自动生成当前时间或初始结果，让工具面板进入即有可用输出。
- 更新 README 展示素材与公开仓库忽略规则，移除不再使用的本地临时展示图。

## English
- Added default auto-run behavior across multiple tool panels: conversions and formatting now run as input changes, reducing manual clicks for JSON, format conversion, Base64, URL, Hash, SQL, XML, Regex, Cron, QR Code, and related tools.
- Improved initial states for timestamp and Snowflake tools so panels show useful current-time or generated output immediately after opening.
- Updated README preview assets and public repository ignore rules, removing obsolete local preview images.
