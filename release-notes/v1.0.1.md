## 中文
- 修复 GitHub Release 工作流中的插件资产打包与上传路径，避免插件发布阶段因找不到 zip 文件而失败。
- 修复桌面端发布工作流中失效的 Tauri Action 版本引用，恢复 macOS、Windows 和 Linux 桌面端构建入口。
- 升级部分 GitHub Actions 依赖版本，处理 Node.js 20 运行时弃用告警。
- README 补充 Docker Compose 与 Docker Run 的 Web 部署说明，并更新固定版本镜像示例。

## English
- Fixed the GitHub Release workflow for extension asset packaging and upload so the extension release step no longer fails on a missing zip file.
- Fixed the desktop release workflow by updating the invalid Tauri Action reference, restoring the macOS, Windows, and Linux desktop build entry points.
- Updated selected GitHub Actions dependencies to address the Node.js 20 runtime deprecation warnings.
- Added Docker Compose and Docker Run deployment instructions to the README and refreshed the fixed-version image example.
