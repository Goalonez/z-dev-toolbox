## 中文
- 重写中英文 README，按开源项目首页方式重组内容，补充 4 张真实界面示例图，去掉面向普通用户无意义的发布实现细节。
- 修复浏览器插件 release zip 的目录结构，解压后可直接得到可导入浏览器的扩展文件，不再额外嵌套一层同名目录。
- 补充 macOS 桌面端首次打开时的签名 / 公证说明，并提供隔离属性移除命令。

## English
- Reworked both README files as project landing pages, added four real screenshots, and removed release-pipeline details that are irrelevant to open-source users.
- Fixed the browser extension release zip so extracting it yields files that can be loaded directly in the browser instead of an extra nested directory.
- Added a clearer macOS desktop note for unsigned and non-notarized builds, including the quarantine removal command.
